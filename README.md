Учебный проект системы управления заявками, реализованный на языке Python с использованием асинхронных технологий.

##  Основные возможности

### Для пользователей:
* **Автоматическая регистрация**: Сбор ФИО, учебной группы и номера телефона через FSM (машину состояний).
* **Создание заявок**: Возможность отправить текстовое описание проблемы и прикрепить одно изображение.
* **История обращений**: Просмотр списка своих заявок и их текущего статуса в реальном времени.

### Для администраторов:
* **Интерактивная админ-панель**: Доступ по Telegram ID, настроенному в окружении.
* **Фильтрация заявок**: Просмотр обращений по статусам (Новые, В обработке, Отвечено, Отклонено).
* **Управление статусами**: Смена состояния заявки через инлайн-кнопки с мгновенным обновлением интерфейса.
* **Прямые ответы**: Отправка сообщения пользователю напрямую из бота.

## 🛠 Технологический стек (ТЗ п. 4.0)
* **Язык**: Python 3.10+.
* **Библиотека бота**: aiogram 3.x (асинхронное взаимодействие с Telegram API).
* **База данных**: SQLite (через SQLAlchemy ORM и асинхронный драйвер aiosqlite).
* **Конфигурация**: python-dotenv (хранение секретов в .env).
* **Логирование**: Стандартная библиотека logging с записью событий в `logs/bot.log`.

## Структура проекта (ТЗ п. 7.0)
```text
TG_BOT/
├── database/           # Модели SQLAlchemy и асинхронные запросы
├── handlers/           # Обработчики команд (admin и user)
├── keyboards/          # Интерактивные Reply и Inline клавиатуры
├── logs/               # Файлы логов системы
├── states/             # Определения состояний FSM
├── .env                # Конфигурационный файл (токен, ID админов)
├── bot.py              # Точка входа, настройка логирования и запуск
└── requirements.txt    # Список зависимостей проекта